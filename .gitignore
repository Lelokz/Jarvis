# ambiente
.venv/
__pycache__/
*.py[cod]

# modelos baixados sob demanda (vozes do Piper ~63MB cada)
vozes/

# medições e áudio bruto da Etapa 0
logs/

# áudio avulso (autoteste, experimentos)
*.wav

# modelos do experimento de wake word (~9MB, baixados sob demanda)
modelos-wakeword/

# Credenciais do Google (Etapa 4). NUNCA commitar: são a chave da conta do Léo.
# credenciais_google.json é o que o console gera; token_google.json é o que a
# autorização grava depois do primeiro consentimento.
credenciais_google.json
token_google.json
